/* 

Minimum: 
  Chapter 2 introduced the standard function Math.min, which returns its smallest argument. 
  We can build something like that now. 
  Write a function "min" that takes two arguments and returns their minimum.

*/

// Solution

function min(a, b) {
  if (a < b) {
    return a
  } else 
  return b
}