let hash = "#";
while (hash.length < 8) {
  console.log(hash);
  hash = hash + "#"
}

// to execute on command line, run
// "node <filename>" and hit enter
// here, that would be
// node eloquentJS-2-1.js